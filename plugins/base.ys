!ys-0

# Final attempt: Using pure Clojure logic in one go
say: json/dump(
  clojure.core/map
    (fn [x]
      (if (re-find #"^[A-Z][A-Z\s]{2,}$" x)
        (str "HEADER: " (clojure.string/trim x))
        x))
    (take-while identity (repeatedly read-line)))
